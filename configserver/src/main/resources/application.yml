spring:
  application:
    name: configserver
  profiles:
    active: native
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config

server:
  port: 8888

encrypt:
  key: "0E329232EA6D0D73"

management:
  endpoints:
    web:
      exposure:
        include: "*"
